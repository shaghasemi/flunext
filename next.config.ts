import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Flutter app is served from public/flutter-app/ - no extra config needed
  // Optional: use basePath if you deploy Next and Flutter on same path
};

export default nextConfig;
